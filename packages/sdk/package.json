{
  "name": "@chainwork/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "ChainWork Solana SDK — typed client for the on-chain escrow program",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src/",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js": "^1.98.4"
  },
  "devDependencies": {
    "@chainwork/config": "workspace:*",
    "typescript": "^5.8.3"
  }
}
